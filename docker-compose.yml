services:
  nuntia:
    build: .
    container_name: nuntia
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - workspaces:/data/workspaces
    environment:
      - WORKSPACE_ROOT=/data/workspaces
      - NODE_ENV=production

volumes:
  workspaces:
